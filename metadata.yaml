# metadata.yaml

# 插件的唯一标识符 (必须与 main.py 中 @register 的第一个参数一致)
name: liar_tavern

# 插件作者的名字或昵称
author: xunxi  # 请替换成你自己的名字

# 插件的版本号 (建议遵循语义化版本规范, 如 1.0.0)
version: 2.1.9

# 插件功能的简短描述
description: 一个在QQ群里玩骗子酒馆的AstrBot插件

# (可选但推荐) 插件代码仓库的 URL 地址
repo: https://github.com/xunxiing/astrbot_plugin_liars_bar # 替换成你实际的仓库地址，如果没有可以不填或留空

# (可选) 插件的标签，方便分类 (这是一个示例，可以根据需要添加)
# tags:
#   - game
#   - fun
#   - social

# (可选) 要求的 AstrBot 最低版本
# requires_astrbot_version: ">=1.5.0"
